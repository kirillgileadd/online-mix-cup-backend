# 📊 Руководство по нагрузочному тестированию

Это руководство поможет вам провести нагрузочное тестирование вашего API с помощью Apache Bench.

## 📋 Содержание

1. [Установка Apache Bench](#установка-apache-bench)
2. [Использование скриптов](#использование-скриптов)
   - [Локальное тестирование](#для-linuxmac-bash)
   - [Тестирование удаленного сервера](#тестирование-удаленного-сервера)
3. [Интерпретация результатов](#интерпретация-результатов)
4. [Мониторинг во время тестирования](#мониторинг-во-время-тестирования)

## 🔧 Установка Apache Bench

### Windows

**Вариант 1: Использование WSL (рекомендуется)**

```bash
# В WSL (Ubuntu)
sudo apt-get update
sudo apt-get install apache2-utils
```

**Вариант 2: Установка Apache HTTP Server**

- Скачайте Apache HTTP Server с официального сайта
- Apache Bench будет доступен в папке `bin`

**Вариант 3: Использование готового бинарника**

- Скачайте `ab.exe` из интернета
- Добавьте в PATH или используйте полный путь

### Linux/Mac

```bash
# Ubuntu/Debian
sudo apt-get install apache2-utils

# Mac
brew install httpd
```

## 🚀 Использование скриптов

### Для Linux/Mac (Bash)

```bash
# Сделать скрипты исполняемыми (если еще не сделано)
chmod +x load-test-advanced.sh analyze-results.sh

# Запустить тестирование с настройками по умолчанию (localhost:8000)
./load-test-advanced.sh

# Запустить с указанием хоста и порта
./load-test-advanced.sh localhost 8000

# Анализ результатов
./analyze-results.sh
# или указать конкретную директорию
./analyze-results.sh load_test_results_20250111_120000
```

### Для Windows (PowerShell)

```powershell
# Запустить тестирование с настройками по умолчанию (localhost:8000)
.\load-test-advanced.ps1

# Запустить с указанием хоста и порта
.\load-test-advanced.ps1 -Host localhost -Port 8000

# Анализ результатов (используйте bash скрипт в WSL)
wsl ./analyze-results.sh
```

### Тестирование удаленного сервера

Для тестирования удаленного сервера используйте скрипт `load-test-remote.sh`:

```bash
# Сделать скрипт исполняемым
chmod +x load-test-remote.sh

# Тестирование напрямую (рекомендуется, минуя Cloudflare)
./load-test-remote.sh 123.45.67.89:8000 direct 8000

# Тестирование через Cloudflare (может быть ограничено)
./load-test-remote.sh api.yourserver.com cloudflare

# Тестирование локально на сервере (через SSH)
ssh user@server "./load-test-remote.sh localhost local 8000"
```

#### Режимы тестирования

1. **`direct`** - Прямое подключение к серверу (минуя Cloudflare)

   - ✅ Точные результаты производительности
   - ✅ Без ограничений Cloudflare
   - ✅ Используется для реальной оценки производительности

2. **`cloudflare`** - Через Cloudflare

   - ⚠️ Может быть ограничено защитой Cloudflare
   - ⚠️ Результаты включают задержку Cloudflare (+50-200ms)
   - ⚠️ Cloudflare может кэшировать GET запросы
   - ⚠️ Ваш IP может быть заблокирован при высокой нагрузке

3. **`local`** - Локальное тестирование на сервере
   - ✅ Самые точные результаты
   - ✅ Исключает влияние сети
   - ✅ Используется для тестирования производительности самого сервера

#### Примеры использования

```bash
# Тест локального сервера
./load-test-remote.sh localhost local 8000

# Тест удаленного сервера по IP (прямое подключение)
./load-test-remote.sh 192.168.1.100 direct 8000

# Тест через домен с Cloudflare
./load-test-remote.sh api.example.com cloudflare

# Тест с полным URL
./load-test-remote.sh https://api.example.com cloudflare
```

#### ⚠️ Важные замечания при тестировании через Cloudflare

- **Rate Limiting**: Cloudflare ограничивает количество запросов с одного IP
- **DDoS Protection**: Высокая нагрузка может быть заблокирована как DDoS атака
- **IP блокировка**: Ваш IP может быть временно заблокирован
- **Кэширование**: GET запросы могут обслуживаться из кэша Cloudflare
- **Задержки**: Cloudflare добавляет 50-200ms к времени ответа

**Рекомендации**:

- Для точного тестирования используйте режим `direct`
- Или создайте поддомен без Cloudflare (серый облачко в DNS)
- Добавьте ваш IP в whitelist Cloudflare, если нужно тестировать через него
- Используйте консервативные настройки нагрузки при тестировании через Cloudflare

## 📈 Что делает скрипт

Скрипт `load-test-advanced.sh` (или `.ps1`) выполняет следующие тесты:

### 1. Тесты с разными уровнями нагрузки

- **Низкая нагрузка**: 10 пользователей, 100 запросов
- **Средняя нагрузка**: 50 пользователей, 500 запросов
- **Высокая нагрузка**: 100 пользователей, 1000 запросов
- **Очень высокая нагрузка**: 200 пользователей, 2000 запросов
- **Экстремальная нагрузка**: 500 пользователей, 5000 запросов

### 2. Тесты разных эндпоинтов

- `/users`
- `/applications`
- `/tournaments`
- `/players`

### 3. Длительный тест на стабильность

- 100 пользователей, 10000 запросов

## 📊 Интерпретация результатов

### Где найти результаты

После выполнения скрипта создается директория с результатами:

```
load_test_results_YYYYMMDD_HHMMSS/
├── SUMMARY.txt              # Сводный отчет
├── test1_low_load.txt       # Детальные результаты теста 1
├── test1_low_load.tsv       # Данные для графика теста 1
├── test2_medium_load.txt    # Детальные результаты теста 2
├── test2_medium_load.tsv    # Данные для графика теста 2
└── ...
```

### Ключевые метрики

#### 1. Requests per second (RPS)

**Где найти**: В файлах результатов, строка `Requests per second`

**Оценка**:

- ✅ **Отлично**: > 1000 RPS
- ✅ **Хорошо**: 100-1000 RPS
- ⚠️ **Плохо**: < 100 RPS

#### 2. Процент успешных запросов

**Где найти**: В файлах результатов, строка `Failed requests`

**Оценка**:

- ✅ **Отлично**: 0 неудачных запросов (100% успешных)
- ⚠️ **Проблема**: Любое количество неудачных запросов

#### 3. Среднее время ответа

**Где найти**: В файлах результатов, строка `Time per request (mean)`

**Оценка**:

- ✅ **Отлично**: < 100ms
- ✅ **Хорошо**: 100-500ms
- ⚠️ **Плохо**: > 500ms

#### 4. Максимальное время ответа

**Где найти**: В файлах результатов, таблица `Connection Times`, колонка `max` в строке `Total`

**Оценка**: Должно быть разумным (не более 2-3 секунд для большинства запросов)

### Пример вывода Apache Bench

```
Requests per second:    426.44 [#/sec] (mean)  ← RPS
Time per request:       117.250 [ms] (mean)    ← Среднее время
Time per request:       2.345 [ms] (mean, across all concurrent requests)
Transfer rate:          513.45 [Kbytes/sec] received

Connection Times (ms)
              min  mean[+/-sd] median   max
Connect:        0    1   0.5      1       3
Processing:    45  115  25.3    110     250
Waiting:       45  115  25.3    110     250
Total:         45  116  25.5    111     251  ← Максимальное время

Percentage of the requests served within a certain time (ms)
  50%    111
  66%    120
  75%    125
  80%    130
  90%    145
  95%    160
  98%    180
  99%    200
 100%    251 (longest request)
```

## 🔍 Мониторинг во время тестирования

### Просмотр логов приложения

**Windows PowerShell:**

```powershell
# Все логи
Get-Content logs/app.log -Wait -Tail 50

# Только ошибки
Get-Content logs/app.log -Wait -Tail 50 | Select-String -Pattern "error|Error|ERROR"
```

**Linux/Mac:**

```bash
# Все логи
tail -f logs/app.log

# Только ошибки
tail -f logs/app.log | grep -i error
```

### Мониторинг ресурсов

**Windows PowerShell:**

```powershell
# CPU и память процесса Node.js
Get-Process node | Select-Object CPU, WorkingSet, Id | Format-Table

# Постоянный мониторинг
while ($true) {
    Clear-Host
    Get-Process node | Select-Object CPU, WorkingSet, Id | Format-Table
    Start-Sleep -Seconds 2
}
```

**Linux/Mac:**

```bash
# Использование ресурсов
top
# или
htop

# Только процесс Node.js
top -p $(pgrep -f node)
```

## 📝 Рекомендации по тестированию

1. **Начните с малой нагрузки** - убедитесь, что базовые тесты проходят успешно
2. **Постепенно увеличивайте нагрузку** - наблюдайте, как система справляется
3. **Мониторьте ресурсы** - следите за CPU, памятью и сетью
4. **Проверяйте логи** - ищите ошибки и предупреждения
5. **Тестируйте разные эндпоинты** - некоторые могут быть более нагруженными
6. **Проводите тесты в условиях, близких к production** - используйте похожую конфигурацию

## 🎯 Оценка результатов для планирования ресурсов

### Пример расчета

Если ваше приложение показывает:

- **500 RPS** при 100 параллельных пользователях
- **Среднее время ответа: 200ms**
- **0% ошибок**

Это означает:

- Приложение может обрабатывать **500 запросов в секунду**
- При среднем времени сессии пользователя **5 минут** (300 секунд)
- Один пользователь делает примерно **10 запросов в минуту** (1 запрос каждые 6 секунд)
- **500 RPS / 10 запросов в минуту = 50 пользователей в секунду**
- **50 пользователей/сек × 300 сек = 15,000 одновременных пользователей**

**⚠️ Внимание**: Это упрощенный расчет. Реальная нагрузка зависит от:

- Типа запросов (GET легче, чем POST)
- Сложности операций
- Использования базы данных
- Кэширования
- И других факторов

### Рекомендации по ресурсам сервера

**Для 100-500 RPS:**

- CPU: 2-4 ядра
- RAM: 2-4 GB
- Хорошее сетевое соединение

**Для 500-1000 RPS:**

- CPU: 4-8 ядер
- RAM: 4-8 GB
- Быстрое сетевое соединение

**Для >1000 RPS:**

- CPU: 8+ ядер
- RAM: 8+ GB
- Оптимизация кода и базы данных
- Возможно, нужна горизонтальная масштабируемость

## 🐛 Решение проблем

### Проблема: Много неудачных запросов

**Возможные причины:**

- Сервер перегружен
- Таймауты слишком короткие
- Проблемы с базой данных
- Недостаточно ресурсов

**Решение:**

- Уменьшите количество параллельных соединений
- Увеличьте таймауты в Apache Bench (`-s` параметр)
- Проверьте логи приложения
- Увеличьте ресурсы сервера

### Проблема: Медленное время ответа

**Возможные причины:**

- Медленные запросы к базе данных
- Отсутствие кэширования
- Неоптимальные запросы
- Недостаточно ресурсов

**Решение:**

- Оптимизируйте запросы к базе данных
- Добавьте кэширование
- Проверьте индексы в базе данных
- Используйте connection pooling

### Проблема: Apache Bench не запускается

**Windows:**

- Убедитесь, что `ab.exe` доступен в PATH
- Или используйте WSL

**Linux/Mac:**

- Установите `apache2-utils` (Linux) или `httpd` (Mac)

### Проблема: "SSL not compiled in; no https support"

Эта ошибка означает, что ваша версия Apache Bench не поддерживает HTTPS соединения.

**Решения:**

1. **Используйте HTTP вместо HTTPS** (рекомендуется для тестирования):

   ```bash
   # Вместо HTTPS используйте HTTP
   ./load-test-remote.sh api.example.com direct 8000
   # или явно укажите HTTP
   ./load-test-remote.sh http://api.example.com direct 8000
   ```

2. **Используйте альтернативный скрипт на curl** (поддерживает HTTPS):

   ```bash
   chmod +x load-test-http.sh
   ./load-test-http.sh https://api.example.com/users 1000 20
   ```

3. **Установите Apache Bench с поддержкой SSL**:

   ```bash
   # Ubuntu/Debian - установите из исходников или используйте альтернативные инструменты
   # Или используйте wrk или k6, которые поддерживают HTTPS из коробки
   ```

4. **Используйте альтернативные инструменты**:
   - **wrk**: `wrk -t4 -c100 -d30s https://api.example.com/users`
   - **k6**: Современный инструмент для нагрузочного тестирования с поддержкой HTTPS
   - **curl** (через скрипт `load-test-http.sh`)

## 📚 Дополнительные ресурсы

- [Документация Apache Bench](https://httpd.apache.org/docs/2.4/programs/ab.html)
- [Руководство по нагрузочному тестированию](https://k6.io/docs/)

## 💡 Советы

1. **Тестируйте постепенно** - не начинайте с максимальной нагрузки
2. **Сохраняйте результаты** - они помогут отследить изменения производительности
3. **Сравнивайте результаты** - после оптимизаций запускайте те же тесты
4. **Тестируйте в production-подобных условиях** - используйте похожую конфигурацию
5. **Мониторьте все метрики** - не только RPS, но и время ответа и ошибки

---

**Удачи с тестированием! 🚀**
